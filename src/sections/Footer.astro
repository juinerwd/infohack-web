---
const currentYear = new Date().getFullYear();

const footerLinks = {
  legal: [
    { name: 'Aviso Legal', href: '/aviso-legal' },
    { name: 'Política de Privacidad', href: '/privacidad' },
    { name: 'Responsible Disclosure', href: '/responsible-disclosure' },
  ],
  sections: [
    { name: 'Protege tus datos', href: '/guides' },
    { name: 'Tutoriales', href: '/tutorials' },
    { name: 'Noticias', href: '/news' },
    { name: 'Recursos', href: '/resources' },
  ],
  social: [
    { name: 'GitHub', href: 'https://github.com/juinerwd/infohack-web', icon: '⚡' },
    { name: 'Contacto', href: '/contact', icon: '📧' },
  ]
};
---

<footer class="bg-slate-800 text-white">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
    <div class="grid grid-cols-1 md:grid-cols-4 gap-8">
      <!-- Logo y descripción -->
      <div class="col-span-1 md:col-span-2">
        <div class="flex items-center space-x-2 mb-4">
          <span class="text-2xl">🛡️</span>
          <span class="text-xl font-bold">Infohack</span>
        </div>
        <p class="text-gray-300 text-sm mb-4 max-w-md">
          Plataforma educativa sobre ciberseguridad. Contenido accesible para usuarios comunes y tutoriales éticos para principiantes.
        </p>
        <div class="flex items-center space-x-4">
          {footerLinks.social.map(link => (
            <a 
              href={link.href}
              class="text-gray-300 hover:text-white transition-colors text-sm flex items-center space-x-1"
              target={link.href.startsWith('http') ? '_blank' : '_self'}
              rel={link.href.startsWith('http') ? 'noopener noreferrer' : ''}
            >
              <span>{link.icon}</span>
              <span>{link.name}</span>
            </a>
          ))}
        </div>
      </div>

      <!-- Secciones -->
      <div>
        <h3 class="text-sm font-semibold text-gray-100 uppercase tracking-wider mb-4">
          Secciones
        </h3>
        <ul class="space-y-2">
          {footerLinks.sections.map(link => (
            <li>
              <a 
                href={link.href}
                class="text-gray-300 hover:text-white transition-colors text-sm"
              >
                {link.name}
              </a>
            </li>
          ))}
        </ul>
      </div>

      <!-- Legal -->
      <div>
        <h3 class="text-sm font-semibold text-gray-100 uppercase tracking-wider mb-4">
          Legal
        </h3>
        <ul class="space-y-2">
          {footerLinks.legal.map(link => (
            <li>
              <a 
                href={link.href}
                class="text-gray-300 hover:text-white transition-colors text-sm"
              >
                {link.name}
              </a>
            </li>
          ))}
        </ul>
      </div>
    </div>

    <!-- Disclaimer ético -->
    <div class="border-t border-gray-700 mt-8 pt-8">
      <div class="bg-amber-900/20 border border-amber-700/30 rounded-lg p-4 mb-6">
        <p class="text-amber-200 text-sm">
          <strong>⚠️ Disclaimer ético:</strong> Todo el contenido de Infohack tiene fines educativos. 
          El uso de estas técnicas debe realizarse únicamente en sistemas propios o con autorización explícita. 
          El uso indebido puede ser ilegal.
        </p>
      </div>
      
      <div class="flex flex-col md:flex-row justify-between items-center text-sm text-gray-400">
        <p>
          © {currentYear} Infohack. Licencia MIT. Contenido educativo sobre ciberseguridad.
        </p>
        <p class="mt-2 md:mt-0">
          Hecho con ❤️ para democratizar la ciberseguridad
        </p>
      </div>
    </div>
  </div>
</footer>
