---
export interface Props {
  title?: string;
  subtitle?: string;
  primaryCTA?: {
    text: string;
    href: string;
    icon?: string;
  };
  secondaryCTA?: {
    text: string;
    href: string;
    icon?: string;
  };
}

const {
  title = 'Aprende a proteger tus datos y a navegar seguro',
  subtitle = 'Guías prácticas para proteger tus datos y tutoriales éticos para principiantes. Todo el contenido es educativo y responsable.',
  primaryCTA = {
    text: 'Soy usuario común',
    href: '/guides',
    icon: ''
  },
  secondaryCTA = {
    text: 'Quiero aprender',
    href: '/tutorials', 
    icon: ''
  }
} = Astro.props;
---

<section class="text-white" style="background: linear-gradient(135deg, #0f1724 0%, #075985 100%)">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-20 lg:py-32">
    <div class="max-w-4xl mx-auto text-center">
      <h1 class="text-4xl md:text-6xl font-bold mb-6 text-balance text-white">
        {title}
      </h1>
      
      <p class="text-xl md:text-2xl text-gray-200 mb-12 max-w-3xl mx-auto text-balance">
        {subtitle}
      </p>
      
      <div class="flex flex-col sm:flex-row gap-4 justify-center items-center">
        <a
          href={primaryCTA.href}
          class="bg-green-600 hover:bg-green-700 text-white px-8 py-4 text-lg font-semibold rounded-lg transition-all duration-200 flex items-center space-x-2 shadow-xl hover:shadow-2xl"
        >
          {primaryCTA.icon && <span class="text-xl">{primaryCTA.icon}</span>}
          <span>{primaryCTA.text}</span>
        </a>
        
        <!-- <a
          href={secondaryCTA.href}
          class="bg-white/10 hover:bg-white/20 text-white border border-white/30 px-8 py-4 text-lg font-semibold rounded-lg transition-all duration-200 flex items-center space-x-2 backdrop-blur-sm"
        >
          {secondaryCTA.icon && <span class="text-xl">{secondaryCTA.icon}</span>}
          <span>{secondaryCTA.text}</span>
        </a> -->
      </div>
      
      <!-- Características destacadas -->
      <div class="mt-16 grid grid-cols-1 md:grid-cols-3 gap-8 text-center">
        <div class="bg-white/10 backdrop-blur-sm rounded-lg p-6 border border-white/20">
          <div class="text-3xl mb-3">🔒</div>
          <h3 class="text-lg font-semibold mb-2 text-white">Para usuarios comunes</h3>
          <p class="text-gray-200 text-sm">
            Guías simples para proteger contraseñas, detectar phishing y navegar seguro
          </p>
        </div>
        
        <div class="bg-white/10 backdrop-blur-sm rounded-lg p-6 border border-white/20">
          <div class="text-3xl mb-3">🎯</div>
          <h3 class="text-lg font-semibold mb-2 text-white">Contenido ético</h3>
          <p class="text-gray-200 text-sm">
            Tutoriales de hacking ético con disclaimers y enfoque educativo
          </p>
        </div>
        
        <div class="bg-white/10 backdrop-blur-sm rounded-lg p-6 border border-white/20">
          <div class="text-3xl mb-3">🚀</div>
          <h3 class="text-lg font-semibold mb-2 text-white">Siempre actualizado</h3>
          <p class="text-gray-200 text-sm">
            Noticias de ciberseguridad y recursos actualizados semanalmente
          </p>
        </div>
      </div>
    </div>
  </div>
  
  <!-- Gradient overlay para transición suave -->
  <div class="bg-gradient-to-b from-transparent to-white h-5"></div>
</section>
